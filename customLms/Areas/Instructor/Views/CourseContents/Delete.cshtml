@model customLms.Areas.Instructor.Models.CourseContentModel
@using customLms.Areas.Instructor.Models

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>CourseContent</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(m => m.contentName)</dt>
        <dd>@Html.DisplayFor(m => m.contentName)</dd>
        <dt>@Html.DisplayNameFor(m => m.courseName)</dt>

        <dd>@Html.DisplayFor(m => m.courseName)</dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
